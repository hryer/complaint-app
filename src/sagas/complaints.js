import { call, put } from 'redux-saga/effects'
import { REQUEST_COMPLAINT_SUCCESS, REQUEST_COMPLAINT_FAILED } from 'actions/types';
import { requestComplaint } from 'api/complaints';
import * as NavigationService from 'libs/navigation/NavigationServices.js'

export function* fetchComplaints(actions) {
  try {
  
  }
  catch (errData) {
    
  }
}